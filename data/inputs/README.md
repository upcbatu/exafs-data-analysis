# Inputs

These are the committed analysis inputs used by CI:

- `atoms.inp`: Cu fcc crystallographic input for Atoms.
- `cu.prj.gz`: Cu foil project file containing the three temperature datasets.

The pipeline treats these as read-only.
